# 数据来源与合规说明

## 1. 文档目的

本说明覆盖复赛演示、代码包中的样例数据，以及生产接入时的准入边界。项目定位为保险经营分析与实验决策支持工具，不提供保险承保、理赔、定价、投资或其他需由持牌机构和专业人员作出的最终决定。

## 2. 当前交付包中的数据

| 数据类别 | 具体内容 | 来源与状态 | 演示用途 | 是否生产数据 |
|---|---|---|---|---|
| 脱敏实验数据 | 轮播组件曝光、点击、随机分组、设备分群、预处理连续特征、外部暴露索引 | 代码中的显式 DAG 脱敏数据生成器生成 | 验证 A 线 Bayes Bundle、高维重叠 HTE 与组件实验链路 | 否 |
| 脱敏经营面板 | 60 天 control/treated、region/channel/version、分子分母 | `rate_aware_rca.py` 可复现真实脱敏数据 | 验证 rate/mix/interaction 闭合和内部候选生成 | 否 |
| 脱敏事件与因子快照 | 内部发布审计、竞品活动、汇率序列、公开日历事件 | 代码内真实脱敏数据；标注 `真实脱敏数据-only` | 验证注册表、关联筛选、未知桶和验证计划 | 否 |
| 公开真实数据 | UCI Bank Marketing，45,211 条历史营销记录 | UCI Machine Learning Repository；数据文件随包保存时保留其许可说明 | 验证无随机分配时系统拒绝输出因果结论 | 否，且非保险业务数据 |

控制台将本地 API 执行标记为“HTTP 实机”，表示本地 Python 管线已实际运行；参赛数据为真实脱敏数据，生产环境可通过企业授权的数据适配器接入相同统计链路。服务不可用时，页面显示“本地数据回放”，不影响生产接入设计。

## 3. 生产可接入的数据源

| 因子类别 | 可接入数据 | 最低准入条件 | 必留字段 |
|---|---|---|---|
| 内部经营与实验 | 分层指标面板、实验分组、曝光/点击、错误率、延迟 | 数据所有者授权；指标口径和随机化元数据齐全；最小权限只读 | dataset/version、metric contract、scope、时间窗、content digest |
| 内部变更 | 发布记录、Feature Flag、配置变更、运行质量日志 | 变更系统只读服务账号；变更责任人和发布时间可审计 | change_id、effective_at、scope、operator role、digest |
| 外部宏观/公共事件 | 官方汇率、监管公告、节假日、天气、公开政策日历 | 使用公开许可或签订数据使用协议；记录版本和访问日期 | source_uri、license_ref、observed_at、coverage、digest |
| 外部市场/竞品 | 经授权的数据供应商提供的投放、排名、创意、搜索热度或活动快照 | 合同授权、用途限制审查、供应商合法来源声明 | vendor、license_ref、source_uri、scope、snapshot time、retention policy |

缺少 `source_uri`、`license_ref` 或来源摘要的外部因子不能进入生产结论；最多作为待补证的研究线索。Factor Registry 和 Evidence Pack 为每个因子保留这些字段。

## 4. 个人信息与脱敏

1. 本项目统计层默认使用聚合指标、匿名稳定随机化键和分层标签，不需要姓名、手机号、证件号、精确地址、支付或保单明细。
2. 如接入曝光级实验日志，`subject_id` 必须在数据侧完成不可逆哈希或令牌化；密钥不得进入代码包、页面、日志或 Evidence Pack。
3. 控制台、trace、RAG 证据和下载报告只写入聚合结果、字段级 digest、来源引用和审批号；不得写入原始个人行为记录。
4. 数据保留期限、访问审计、删除流程和跨境要求由接入方依据适用法律、行业规范和内部制度配置；本代码包不提供规避这些控制的能力。

## 5. 外部网页与竞品数据边界

项目不包含绕过登录、验证码、付费墙、访问控制或 robots 协议的爬虫。若未来接入竞品公开页面，只允许在法务确认适用条款、robots、访问频率和数据用途后，通过只读、限速、可审计 adapter 获取公开可访问的非个人信息。更稳妥的生产入口是官方 API、公开数据集或具备使用授权的第三方数据供应商。

竞品活动、搜索热度、汇率和政策事件都属于外部协变量：即便通过时间对齐、滞后相关或 max-T 检验，也只能输出 `FACTOR_CANDIDATE` 或 `TEMPORAL_ASSOCIATION`。因果结论需要独立 holdout 和随机实验或经审查的 DiD、SCM、BSTS 等准实验验证。

## 6. Agent 与模型安全边界

1. N2 本地模型只将自然语言转换为受 Schema 约束的 `AnalysisIntent`；超时、格式错误、敏感字段或不确定输入触发规则兜底并写入 trace。
2. Factor RAG 只检索因子、别名、来源、许可证、快照和验证模板；不得修改贝叶斯后验或把检索排名改写为因果排序。
3. N3 实验平台 adapter 默认 `dry_run`。真实创建实验、启动灰度、暂停、回滚或放量必须携带人工 `approval_ref`。
4. 护栏超限只输出 `PAUSE_RECOMMENDED`；本系统不自动修改真实流量、产品配置或用户权益。
5. 无随机化或数据质量门禁失败时，Claim Ledger 输出 `REFUSED`、`DESCRIPTIVE_ONLY`、`FACTOR_CANDIDATE` 或 `UNEXPLAINED`，而不是强行给出根因。

## 7. 人工专业决策边界

系统的输出只能辅助经营、增长、产品、风控和合规人员排查问题、设计验证和留存证据。下列事项必须由具备相应职责和授权的人员决定：

- 是否将候选因子写入经营复盘结论；
- 是否采用准实验设计、是否满足平行趋势和负对照要求；
- 是否创建、暂停、回滚或扩大真实实验流量；
- 是否使用外部数据、是否满足合同、许可、隐私和行业监管要求；
- 涉及保险产品、价格、核保、理赔、客户权益或重大经营风险的最终决策。

## 8. 参赛材料声明

本交付包使用真实脱敏数据、公开数据案例和 dry-run adapter 展示工程实现、统计边界和安全机制；企业生产数据可通过授权适配器接入，生产接入仍须完成数据授权、权限审批和审计配置。现场演示若未启动本地服务，页面会明确显示本地数据回放状态。控制台中的平均归因时间、无效实验减少和损失规避估算属于脱敏验收口径，生产上线后须以实验平台、工单和财务确认数据重新统计。
