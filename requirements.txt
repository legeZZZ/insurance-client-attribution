# HTTP 服务（run_server.py / 控制台）运行所需的最小依赖。
# 服务链路全部为标准库 + numpy 懒加载；Render 等云平台部署只需本文件。
numpy>=1.24

# 深度分析扩展（PCMCI 因果发现 / DoWhy GCM / EconML 因果森林）为懒加载，
# 仅在本地跑阶段E-F评测与反事实引擎时需要，见 requirements-research.txt。
