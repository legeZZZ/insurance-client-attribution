# 复赛升级运行说明

版本：v12.0，已同步上游 `bc6f688`；服务对象为保险公司经营分析、增长和产品运营人员。

## 1. 环境

Python 3.9+（推荐 Python 3.12），依赖只有 `numpy`；Factor Registry 使用 Python 标准库 SQLite。代码包目录执行命令。

## 2. 一键运行升级 Demo

### 2.1 演示界面

推荐启动本地服务后打开 `http://127.0.0.1:8765`：

```bash
python3 run_server.py 8765
```

根路径直接进入复赛现场控制台。页面初始没有最终结论；用户填写经营问题并选择场景后，主按钮只发起一次意图请求和一次 `GET /api/track2/scenario-run`，同一份 API 结果同步到三维关系图、6 个可视化阶段、轨迹和结果区。三维关系图支持悬浮查看内部证据、外部信号、未知残差和验证路径；候选因子库同步可视化，并明确区分候选关联、时间/范围关联和验证路径。默认 `full_review` 会真实运行 B 线候选挖掘、A 线实验、外部事件映射和 N1/N2/N3 dry-run，并将 60 天 × 8 个切面的 480 行原始面板、240 条因子快照、残差趋势、分层热力、候选因子库和验证计划汇总到一个结果页。原始面板首屏展示 48 行，支持展开查看全部行。

未启动服务时也可以直接打开 `web/static/semifinal-demo.html`。此时页面仍可完整演示，并明确标记为 `本地数据回放`；启动服务后切换为 `HTTP 实机`。参赛数据为真实脱敏数据，生产环境通过企业授权适配器接入。`east / paid / 8.4 · 状态：UNEXPLAINED，未解释阶跃保留` 表示系统按治理规则保留未知量，不是执行失败；真实管线异常会由 API 返回结构化 JSON 错误。

### 2.2 脱敏数据回放

```bash
python3 -m track2_v5.replay_upgrade_demo
```

Demo 完全离线，使用可回放的三类适配器：N1 授权事件源 真实脱敏数据 + Factor RAG；N2 本地模型 schema 校验和失败规则兜底；N3 实验平台 dry-run adapter，并演示 5% 灰度和护栏触发后的暂停建议。同一适配器可接入生产授权数据源。

输出文件：

```text
outputs/upgrade_demo_evidence.json
outputs/upgrade_demo_report.md
```

## 3. 预期结果

终端至少应看到：

- `mapping_coverage = 0.5000`
- `llm_fallback_used = true`
- `canary_percent = 5`
- `guardrail_status = BREACHED`
- `recommended_action = PAUSE_RECOMMENDED`

这说明升级能力接入了现有治理边界，而不是绕过治理边界。

## 4. 原有控制台

```bash
python3 run_server.py 8765
```

浏览器打开 `http://127.0.0.1:8765`。入口为复赛现场控制台：B 线开放因子挖掘、A 线 UI 改版实验、外部事件映射、无随机化拒答都可从初始状态独立运行；本地模型超时、外部源不可用和护栏超限可通过页面故障开关演示。

## 5. 统计与 B 线升级回放

```bash
# 概率差尺度 HTE、高维重叠 CATE 与双层嵌套收缩基准（默认 normal_normal）
python3 -m track2_v5.nested_benchmark

# B 线关联因子发现：内部/外部事件 + 因子快照时序
python3 -m track2_v5.association_discovery

# 内部多维 rate 指标候选生成：分子/分母保留，Squeeze 风格子空间搜索
python3 -m track2_v5.rate_aware_rca

# v12 回归测试（含高维 CATE、重叠 subgroup、控制台契约）
PYTHONPATH=. python3 -m unittest discover -s tests -v
```

输出：`outputs/demo_evidence.json`、`outputs/nested_ablation_50seeds.json`、`outputs/lineB_association_discovery.json`。A 线聚合决策仍由 Beta-Binomial 给出；行级 HTE 使用 `crossfit_overlap_ridge_cate`，将预处理连续变量标准化并加入二次项、hinge 与 pairwise 组合项，允许同一曝光同时命中多个 subgroup，并按 membership 做有效样本校正。旧的 HTE 默认在概率差尺度使用 `normal_normal` 部分池化，作为可比基线保留；嵌套模型在当前小样本基准中方向召回更高，但 moderation RMSE 可能高于 flat，因此生产默认需经过留出预测与校准门禁选择。B 线输出候选关联，不输出因果；没有数据源的外部因子不会被凭空生成。当前 API 使用 `key_outputs`，RCA 候选的当前闭合字段为 `candidate_closure_error`。
多维候选生成额外输出 `outputs/lineB_rate_aware_rca.json`，用于验证 `region/channel/version` 等分层组合是否能定位异常。其总体和候选级输出都采用概率差尺度的 `rate/mix/interaction` 三重分解，三项之和必须闭合；不完整分群集合会标记 `DECOMPOSITION_NOT_CLOSED`。

关联输出使用去趋势/周季节项 + moving-block 独立零分布，并计算锁定候选集合的 max-T；BH q 值仅作辅助报告。`search_manifest` 记录父因子数、派生视图数、范围数、滞后数、指标数、实际比较数、block length、bootstrap 次数、`holdout_survivors`。序列候选的 `factor_id` 始终保持父因子，`derived_feature_id` 才记录 `factor.level / factor.velocity / factor.acceleration` 这样的派生视图，避免把一次/二次求导误注册为新因子。传入独立 `discovery_days` 和 `holdout_days` 后，holdout 是跨滞后选择的主要防线。

## 6. Factor RAG 最小回放

`FactorRegistry` 的最小存储是 SQLite，保存因子定义、别名、适用范围、证据、快照、来源 URL、摘要和许可证。检索只返回候选和证据，不写入因果结论。

```python
from track2_v5.factor_store import FactorStore
from track2_v5.factor_retriever import retrieve_factor_candidates

store = FactorStore("outputs/factors.sqlite3")
store.register_factor({"factor_id": "external.fx", "name": "汇率",
                       "source_type": "authorized_external",
                       "license_ref": "source-license"})
store.ingest_evidence({"factor_id": "external.fx", "evidence_type": "official",
                       "source_uri": "https://example.invalid", "license_ref": "source-license"})
print(retrieve_factor_candidates(store, "汇率"))
```

## 7. 生产替换点

当前真实脱敏数据 adapter 只用于工程验证：N1 替换为授权事件源只读 adapter；N2 替换为 Ollama、llama.cpp 或内部 OpenAI-compatible endpoint；N3 替换为实验平台 sandbox adapter。替换 adapter 不应改动 `track2_v5` 的贝叶斯、FactorMiner、实验设计和 Claim Ledger 核心逻辑。

## 8. 故障排查

本地模型不可用时，Demo 应仍然成功，并在 JSON 的 `trace` 中看到 `rule_fallback`。事件源校验失败时，异常保持 `UNEXPLAINED`。实验护栏超阈值时，结果应为暂停建议，不能自动放量。

## 9. v12 同步后的行为变化

- 输入契约在统计计算前检查 NaN/Inf、概率和计数范围、重复行、窗口重叠、非法分群 ID、FDR 与校准参数。
- `student_t` 默认使用 `mu/tau/nu` 联合网格后验；复现旧 plug-in 结果需显式设置 `student_t_hyperparameter_method="plug_in"`。该路径目前只用于校准研究，Gaussian 仍为生产默认。
- 实验完整性由 8 项行级门禁共同决定：SRM、平衡、分流稳定、污染、时间顺序、漏斗、cluster 和并发实验；任一失败都不授权因果估计。
- 功效规划必须声明 absolute/relative MDE、primary estimand、randomization unit 和 analysis unit，并支持不等分流、cluster、attrition、non-inferiority 与 equivalence。
- `max_arms` 不足时直接拒答，不再静默截断；这属于有意的 fail-closed 兼容性变化。
