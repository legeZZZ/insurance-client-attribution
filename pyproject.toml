[project]
name = "insurance-client-attribution"
version = "0.1.0"
description = "Evidence-governed Bayesian attribution for insurance sales analytics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24",
]

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "ruff>=0.16.5",
    "seaborn>=0.13.2",
    "pandas-stubs~=3.0.5"
]
