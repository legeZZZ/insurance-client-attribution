[project]
name = "insurance-client-attribution"
version = "0.1.0"
description = "Python environment for insurance client attribution"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "ruff>=0.16.5",
]
